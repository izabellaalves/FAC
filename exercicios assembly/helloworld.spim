# data é a area onde definimos variaveis
.data
    msg: .asciiz "Olá, mundo!" 

# text é a area onde colocamos nosas instruções
.text
    main:
    li $v0, 4 #instrução para imprimir uma string
    la $a0, msg
    syscall